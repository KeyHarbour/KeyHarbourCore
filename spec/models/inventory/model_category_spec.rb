require 'rails_helper'

RSpec.describe Inventory::ModelCategory, type: :model do
  # describe 'Tests de robustesse (Stress Tests)' do  
  #   let(:user) { User.new(email: "test@example.com", name: "Jean Dupont") }

  #   # 1. Test de la limite de taille (Overload)
  #   it 'doit rejeter un nom dépassant 1000 caractères' do
  #     user.name = "a" * 1001
  #     expect(user).not_to be_valid
  #   end
  # end
end
